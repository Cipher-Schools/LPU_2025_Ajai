# **Problem 2: Reverse String (Easy)**

🔗 **Link:** [https://leetcode.com/problems/reverse-string/](https://leetcode.com/problems/reverse-string/)

### **📝 Problem Statement**

You are given a character array `s`.

You need to reverse the array **in-place** using **recursion only** (no loops allowed).

Example:

Input: `['h','e','l','l','o']`

Output: `['o','l','l','e','h']`

### **💡 Hint:**

* Use a  **two-pointer recursive approach** .
* Swap `s[left]` and `s[right]`.
* Recursively call with:

  `left + 1` and `right - 1`.
* Stop when `left >= right`.
