# **Problem 1: Fibonacci Number (Easy)**

🔗 **Link:** [https://leetcode.com/problems/fibonacci-number/](https://leetcode.com/problems/fibonacci-number/)

### **📝 Problem Statement**

The Fibonacci sequence is defined as:

<pre class="overflow-visible!" data-start="331" data-end="387"><div class="contain-inline-size rounded-2xl relative bg-token-sidebar-surface-primary"><div class="sticky top-9"><div class="absolute end-0 bottom-0 flex h-9 items-center pe-2"><div class="bg-token-bg-elevated-secondary text-token-text-secondary flex items-center gap-4 rounded-sm px-2 font-sans text-xs"></div></div></div><div class="overflow-y-auto p-4" dir="ltr"><code class="whitespace-pre!"><span><span>F</span><span>(</span><span>0</span><span>)</span><span></span><span>=</span><span></span><span>0</span><span>  
</span><span>F</span><span>(</span><span>1</span><span>)</span><span></span><span>=</span><span></span><span>1</span><span>  
</span><span>F</span><span>(</span><span>n</span><span>)</span><span></span><span>=</span><span></span><span>F</span><span>(</span><span>n − </span><span>1</span><span>)</span><span></span><span>+</span><span></span><span>F</span><span>(</span><span>n − </span><span>2</span><span>)</span><span>
</span></span></code></div></div></pre>

Given `n`, return the `n`-th Fibonacci number using  **recursion** .

### **💡 Hint:**

* Use the formula:

  `fib(n) = fib(n-1) + fib(n-2)`
* Define base cases:

  * `fib(0) = 0`
  * `fib(1) = 1`
* Just make recursive calls until base cases are reached.
