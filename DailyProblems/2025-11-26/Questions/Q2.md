# **📝 Problem 2: Contains Duplicate II**

link: [Link to Problem](https://leetcode.com/problems/contains-duplicate-ii/description/)

## **Problem Statement**

Given an integer array `nums` and an integer `k`, return `true` if there exist **two distinct indices** `i` and `j` such that:

* `nums[i] == nums[j]`
* `|i - j| ≤ k`

Otherwise, return `false`.

---

## **Example**

**Input:**

`nums = [1, 0, 1, 1], k = 1`

**Output:**

`true`

**Explanation:**

`nums[2]` and `nums[3]` are equal and only 1 index apart.

---

## **Constraints**

* `1 ≤ nums.length ≤ 10⁵`
* `-10⁹ ≤ nums[i] ≤ 10⁹`
* `0 ≤ k ≤ 10⁵`
